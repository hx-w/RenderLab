[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "remesh-backend"
version = "0.0.2"
requires-python = ">=3.6"
authors = [{name = "CarOL", email = "herixth@163.com"}]
description = "A backend for remeshing 3D meshes."